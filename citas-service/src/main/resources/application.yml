server:
  port: 8087

spring:
  application:
    name: citas-service

  datasource:
    url: jdbc:mysql://localhost:3306/bd_citas
    username: root
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect