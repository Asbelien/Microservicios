server:
  port: 8085

spring:
  application:
    name: paciente-service

  datasource:
    url: jdbc:mysql://localhost:3306/bd_paciente
    username: root
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect